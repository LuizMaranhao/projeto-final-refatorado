# Projeto de Refatoração: Cadastro de Clientes (POO & Modules) 🚀

Este projeto representa a evolução de uma aplicação de cadastro, focando em **organização**, **reutilização** e **boas práticas** de desenvolvimento moderno em JavaScript.

## 🛠️ Tecnologias e Conceitos Aplicados

Nesta versão, o código foi totalmente reestruturado para aplicar:

* **Programação Orientada a Objetos (POO)**: Criação de classes para representar os dados do sistema.
* **ES Modules**: Divisão do código em arquivos separados (`classes.js`, `utils.js`, `app.js`) usando `import` e `export`.
* **Programação Funcional**: Uso do método `.map()` para renderizar a tabela de forma dinâmica e funções puras para tratamento de dados.
* **Manipulação Avançada do DOM**: Uso de `addEventListener` para capturar eventos de forma limpa.

## 📁 Estrutura do Projeto

Conforme planejado via terminal:
- `index.html`: Interface da aplicação.
- `css/styles.css`: Estilização moderna e responsiva.
- `js/classes.js`: Definição da classe Cliente.
- `js/utils.js`: Funções auxiliares (como formatação de texto).
- `js/app.js`: Lógica principal e integração dos módulos.

## 🚀 Como visualizar
1. Clone o repositório.
2. Utilize a extensão **Live Server** no VS Code para rodar o projeto em um servidor local (necessário para o suporte a módulos).